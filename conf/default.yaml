global:
  trans: "translation"
  lang: "en"
  httpaddr: ":9005"
cluster:
  # see detail of custom cluster here:
  type: "custom"
  kubeconfig: ""
  config:
    node:
      autocreate: true

#diagnostics:
#  - type: "master-capacity"

exporters:
  - type: "stdout"

