# workload-ha diagnostic

This diagnostic report whether the pods of Deployment, StatefulSet, ReplicaSets or ReplicationControllers are deployed on the same node

# config
```yaml
diagnostics:
- type: "workload-ha"
```
# supported cloud providers
* all