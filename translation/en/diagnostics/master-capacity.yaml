title: "Master node {{.Resource}} capacity"
desc: "Master({{.NodeName}}) with {{.CurValue}} {{.Resource}} is insufficient for cluster with {{.NodeTotal}} nodes"
proposal: "{{.TargetValue}} {{.Resource}} needed for cluster with {{.NodeTotal}} nodes"
good-desc: "Master({{.NodeName}}) with {{.CurValue}} {{.Resource}} is sufficient for cluster with {{.NodeTotal}} nodes"